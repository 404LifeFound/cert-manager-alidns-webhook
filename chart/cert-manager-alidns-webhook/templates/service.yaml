apiVersion: v1
kind: Service
metadata:
  name: {{ include "cert-manager-alidns-webhook.fullname" . }}
  labels:
    {{- include "cert-manager-alidns-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "cert-manager-alidns-webhook.selectorLabels" . | nindent 4 }}
